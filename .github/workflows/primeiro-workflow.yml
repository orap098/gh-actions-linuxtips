name: Desafio 1

on:
  workflow_dispatch:
   inputs:
      name:
        type: string

jobs:
  Vulnerabilidade Docker:
    runs-on: ubuntu-latest
    steps:
      - name: Docker pull
        run: docker pull ${{ github.event.inputs.message }}

      - name: Lista Imagens dokcer
        run: docker images

      - name: Relatório de Vulnerabilidade da imagem
        uses: docker/scout-action@v1
        with:
          command: cves
          image: $${{ github.event.inputs.message }}
    
      - name: Finalizado
        run: echo 'FINALIZADO'

